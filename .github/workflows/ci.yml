name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout mcp-gateway
        uses: actions/checkout@v4
        with:
          path: mcp-gateway
      - name: Checkout urule (for workspace deps)
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/urule
          path: urule
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: |
            urule/package-lock.json
            mcp-gateway/package-lock.json
      - name: Pre-build urule workspace packages
        run: npm --prefix urule install && npm --prefix urule run build:all
      - name: Install dependencies
        run: npm --prefix mcp-gateway ci --install-links
      - name: Build
        run: npm --prefix mcp-gateway run build
      - name: Test
        run: npm --prefix mcp-gateway test

  docker-publish:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-and-test]
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout mcp-gateway
        uses: actions/checkout@v4
        with:
          path: mcp-gateway
      - name: Checkout urule
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/urule
          path: urule
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: urule/package-lock.json
      - name: Pre-build urule workspace packages
        run: npm --prefix urule install && npm --prefix urule run build:all
      - uses: docker/setup-buildx-action@v3
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/mcp-gateway
          tags: |
            type=ref,event=tag
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: mcp-gateway/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=mcp-gateway
          cache-to: type=gha,scope=mcp-gateway,mode=max
